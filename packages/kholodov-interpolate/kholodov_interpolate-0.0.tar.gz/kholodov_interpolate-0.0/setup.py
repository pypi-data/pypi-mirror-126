from setuptools import setup

setup(name='kholodov_interpolate',
      version='0.0',
      description='dummy projec',
      packages=['kholodov_interpolate'],
      author_email='ksteklo@mail.ru',
      zip_safe=False)
