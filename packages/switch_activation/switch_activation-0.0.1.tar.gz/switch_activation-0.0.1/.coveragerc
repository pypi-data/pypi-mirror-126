[run]
parallel = True
branch = True
source =
    switch_activation
    tests
omit =
    # Don't complain if non-runnable code isn't run
    */__main__.py

[paths]
source =
    switch_activation
    .tox/*/lib/python*/site-packages/switch_activation
    .tox/pypy/site-packages/switch_activation

[report]
show_missing = True
skip_covered = True
exclude_lines =
    # Have to re-enable the standard pragma
    \#\s*pragma: no cover

    # Don't complain if tests don't hit defensive assertion code:
    ^\s*raise AssertionError\b
    ^\s*raise NotImplementedError\b
    ^\s*return NotImplemented\b
    ^\s*raise$

    # Don't complain if non-runnable code isn't run:
    ^if __name__ == ['"]__main__['"]:$
    ^\s*if False:
    ^\s*if TYPE_CHECKING:
